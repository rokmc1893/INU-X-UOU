# 정책 풀·실시간 수집 구현 계획 (Phase 1~4)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 산업 선택→풀에서 정책 끌어오기(A), URL 실시간 수집(B), 풀 갱신 스크립트(업데이트 경로)를 오늘 밤 안에 앱에 추가한다.

**Architecture:** 스펙 `docs/superpowers/specs/2026-08-13-policy-pool-design.md` 참조. 기존 extract_card·detect·store를 재사용하고, 풀은 파일로 관리해 재실행 갱신이 가능하게 한다.

**Tech Stack:** 기존과 동일 + requests (URL 수집). HTML 파싱은 표준 lib `html.parser`.

## Global Constraints

- 마감 2026-08-14 08:00, 코어는 금일 중. API 키 노출 금지, `.env`만.
- B1 `UNKNOWN` → null 기권, `CONFLICTING:` 수치는 값으로 쓰지 않음, `evidence_status` 뱃지 필수.
- 기존 테스트 23건이 깨지면 안 됨. 각 태스크 끝에 `python -m pytest tests -q`.
- 경로 루트 `C:\workspace\uxi\06_앱\`.

---

## Phase 1 — A: 산업 선택 + 풀에서 끌어오기

### Task 1.1: engine/pool.py — B1 행 → 카드 변환

**Files:** Create `06_앱/engine/pool.py`, `06_앱/tests/test_pool.py`, Copy `data/pool/B1_policy_portfolio.csv` (스크래치패드에서)

**Interfaces:**
- `row_to_pseudo_source(row: dict) -> str` — B1 행을 `# key: value` 메타헤더 + 본문 텍스트로 변환. UNKNOWN → 해당 줄 생략, `CONFLICTING:` 값 → "출처 간 수치 충돌로 미기재" 문장으로 치환
- `convert_industry(industry: str, llm=None) -> list[dict]` — 해당 산업 행들을 extract_card로 변환, 카드에 `stable_policy_id`·`evidence_status`·`pool_version`(=version) 추가, `policy_id`=stable_policy_id
- CLI `python -m engine.pool 바이오` → `data/pool/cards/IC-*.json` 재생성 (= 백그라운드 갱신 진입점)

- [ ] Step 1: 테스트 작성 — `row_to_pseudo_source`가 UNKNOWN 줄을 생략하고 CONFLICTING을 치환하는지, `convert_industry`가 fake llm으로 카드에 stable_policy_id·evidence_status를 붙이는지
- [ ] Step 2: 실패 확인 → 구현 → 통과 확인
- [ ] Step 3: Commit `feat: engine/pool.py — B1 원장→카드 변환기(갱신 CLI)`

### Task 1.2: 바이오 풀 배치 변환 (실 API)

- [ ] Step 1: Run `python -X utf8 -m engine.pool 바이오` → 16건 (바이오 15 + 바이오+디지털데이터 1)
- [ ] Step 2: 눈검수 — IC-BIO-001 시설구축의 stage(해당 없으면 null 허용), IC-BIO-002 교육과정이 [바이오생산·바이오품질]·교육훈련인지, CONFLICTING 예산이 null인지
- [ ] Step 3: Commit `feat: 바이오 정책 풀 카드 16건 (근거등급 표시)`

### Task 1.3: app.py 산업 선택

- [ ] Step 1: 사이드바 `st.selectbox("분석 범위", ["청년일자리(기본)", "청년일자리 + 바이오"])`; init(scope)로 캐시 키 분리; 바이오 선택 시 `data/pool/cards/IC-*.json` 결합 로드
- [ ] Step 2: `label()` 확장 — evidence_status 뱃지: 📰 언론보도 기반, ⚠️ 수치 충돌, 없으면 기존 🟢/🟡. 화면 2 컬럼·화면 3 정책 목록에 그대로 적용. 화면 4 상단에 "현재 분석 범위" 표시
- [ ] Step 3: 브라우저 검증 — 바이오 선택 시 화면 2에 K-NIBRT 교육과정 등장 + 화면 3 바이오 공백이 커버로 바뀌고 인계 공백이 새로 표시되는지
- [ ] Step 4: Commit `feat: 산업 선택 — 풀에서 관련 정책 끌어오기`

## Phase 2 — B: URL 실시간 수집

### Task 2.1: engine/fetch.py

**Interfaces:** `fetch_policy_text(url: str, timeout=10) -> str` — requests.get → `html.parser` 기반 태그 제거·본문 추출 → `# source_url/retrieved_at/data_type: real` 메타헤더 붙인 텍스트. 실패 시 예외 그대로 전파(앱이 폴백 표시)

- [ ] Step 1: 테스트 — 고정 HTML 문자열로 파서 단위 테스트 (`_html_to_text`), 메타헤더 생성 테스트 (네트워크 불필요)
- [ ] Step 2: 실패 확인 → 구현(HTMLParser 서브클래스: script/style 무시, 블록태그 개행) → 통과
- [ ] Step 3: Commit `feat: engine/fetch.py — URL 정책 원문 수집기`

### Task 2.2: app.py URL 가져오기

- [ ] Step 1: 사이드바 expander "URL로 정책 가져오기": text_input + 버튼 → fetch → `extract_card(..., policy_id="U" + 순번)` → 카드 미리보기(st.json 축약) → "분석에 추가" 버튼 → `st.session_state.extra_cards`에 추가, findings 재계산은 session 카드 포함 버전으로
- [ ] Step 2: 실패 폴백 — `st.error("가져오기 실패 — 원문 텍스트를 data/policies/raw에 직접 넣어도 됩니다 (오류: ...)")`
- [ ] Step 3: 실 URL 1건(청년포털 사업 페이지)로 브라우저 검증
- [ ] Step 4: Commit `feat: URL 실시간 수집→추출→분석 추가`

## Phase 3 — 업데이트 경로 문서화 + 마무리

- [x] Task 3.1: 결정로그 D-015 기록 (풀 구조·갱신 경로·근거등급 노출·예상 판정 변화 수용)
- [x] Task 3.2: 전체 pytest(27건) + 4화면 브라우저 검증 + evaluate 재실행 + 커밋·푸시

## Phase 4 — 발표자료

- [x] 데크 8장 제작 (`05_발표자료/build_deck.js` → `정책핏_인천_발표_v1.pptx`, validate 통과)
- [x] 7분 대본 (`07_발표대본_7분.md`) — 5.5자/초 기준 절별 시간 산출(418초) + 컷 규칙
- [ ] **시연 녹화 백업** ← 발표자만 가능. 대체 자산으로 `data/demo_fallback.html` 확보됨

---

# Phase 5 — 감사 반영 및 제출 마감

**근거:** 2026-08-13 밤 실행한 병렬 감사 4건 — ①저장소 구조 ②시연 준비도 ③기록 품질 ④배포 실사용성.
**기한:** 제출 2026-08-14 08:00. 5.1은 제출 전 필수, 5.4는 오늘 하지 않는다.

## 5.1 제출 전 필수 (블로커) — 완료

- [x] **T5.1 데크·대본·백업의 stale 수치 제거** *(기록 감사 §0 — 유일한 "제출 전 반드시" 등급)*
      슬라이드가 `정답셋 31건·기권 100%·인용 89%`를 표시했으나 실측은 `36건·기권 89%·인용 91%`.
      **틀린 쪽이 우리에게 유리한 방향**이라 발표 중 앱 화면과 충돌 시 §7 `UNVERIFIED_CLAIM` 직격.
      → `build_deck.js`·`07_발표대본`·`08_효과측정_설계`·`demo_fallback.html` 전부 실측치로 통일, pptx·snapshot 재생성.
- [x] **T5.2 성능 수치를 증거원장에 등록** *(기록 감사 §2)* — E001~E021에 자체 측정치가 하나도 없어 슬라이드 7 전체가 "원장에 없는 수치" 상태였음. E022(4지표)·E023(v2→v7 추이) 등록, 한계(홀드아웃 없음·자기 채점·표본 구성) 명시.
- [x] **T5.3 누락 결정 5건 기록** *(기록 감사 §1)* — D-018(정답셋 판별력 결함·프롬프트 v7) / D-019(기권 계약) / D-020(3차 폴백) / D-021(모델 이원화) / D-022(URL 카드 그래프 반영·세션 한정).
- [x] **T5.4 URL 수집 카드가 그래프에 안 들어가던 버그** — 파이썬 판정에만 반영되고 Neo4j 미적재. 시그니처 비교 후 재적재로 수정, 노드 10→11·엣지 38→43 실증.
- [x] **T5.5 미커밋 산출물 커밋** *(구조 감사 H5)* — `snapshot.py`·`demo_fallback.html`·pptx 등 19건이 워킹트리에만 있었음.
- [x] **T5.6 근거 범위 초과 표현 삭제** — 대본의 "모든 연령대가 순유입된 **유일한** 시도"(E005 근거는 "모든 연령대에서 순유입"까지). 슬라이드는 이미 정확했고 대본만 넘어감.

## 5.2 제출 전 권장 — 완료

- [x] **T5.7 `06_앱/README.md` 신설** *(구조 감사 H2)* — 실행 명령이 내부 계획 문서에만 있었음. 실행·폴백·재현 명령·데이터 출처와 한계 6절.
- [x] **T5.8 A1 레지스트리 정본 확정** *(구조 감사 H4)* — 조직개편 전후 두 본이 조직명·연락처가 달랐음. 개편 후 판본을 정본으로 두고 F4본은 `_조직개편전`으로 분리, 보도자료 서명자 실명 마스킹.
- [x] **T5.9 `.gitignore` 정정** *(구조 감사 H1·M7)* — `05_발표자료/.gitignore`가 `package.json`까지 막아 데크 빌드가 재현 불가였음. 루트에 `node_modules/`·`.pytest_cache/` 추가.
- [x] **T5.10 F4 조사 원자료 저장소 등록** *(구조 감사 M6)* — A1~A4·B1~B4가 전부 미추적이라 D-015·E020의 근거 문서가 저장소에 없었음.
- [x] **T5.11 보안·재현성** *(배포 감사 §4)* — Streamlit 사용통계 수집 차단(`gatherUsageStats=false`, 공공기관 보안심사 반려 사유), `requirements.txt` 버전 고정.
- [x] **T5.12 사용자 시나리오·기술 동작 문서** — `03_제품설계/사용자시나리오_기술동작.md`. LLM 실행 지점 4곳, 시나리오 5개, 못 하는 것 전수.
- [x] **T5.18 폴백 HTML 한글 깨짐** *(시연 감사 치명 1)* — `<meta charset="utf-8">`·`<html>`·`<body>`가 없어 브라우저가 windows-1252로 디코딩, `정책핏 인천` → `ì •ì±…í• ì¸ì²œ`. **폴백 자산 전체가 사용 불가 상태였음.** 문서 골격 추가로 복구.
- [x] **T5.19 Cypher `gaps` 질의 구문 오류** *(시연 감사 치명 2)* — Neo4j 5.x는 WHERE 패턴식 안의 관계변수 바인딩(`[c:COVERS ...]`)을 금지한다. 심사위원이 실행해 보면 실패했을 질의. `[:COVERS ...]`로 수정 후 **4종 전부 실행 확인**(handoff_breaks 53행 / gaps 0 / harmful 0 / intentional 18).
- [x] **T5.20 "Neo4j에서 실제로 도는 질의" 문구 정정** *(시연 감사 치명 2)* — 판정은 파이썬이 계산하고 Cypher는 동일 논리의 표현이다. 앱 캡션과 대본을 사실대로 고치고, 질의 실행 시 행 수가 화면과 다른 이유(대상·직무 비교가 앱 계층)까지 화면에 명시.
- [x] **T5.21 대본의 시연 실패 절차 현실화** — "즉시 녹화 재생"이었으나 저장소에 영상 파일이 0개다. 녹화 없을 때의 대사와 3차 폴백 경로를 대본에 명시.
- [x] **T5.22 발표 당일 기동 절차** — 대본 말미에 추가. 낡은 인스턴스가 캐시 때문에 옛 데이터를 서비스하던 문제(감사 발견)를 검증 3종으로 차단.

## 5.3 남은 작업 — 사람만 할 수 있는 것

- [ ] **T5.13 시연 녹화 백업** *(루브릭 PRE2 4점 요건)* — 발표 노트북에서 4화면 + 라이브 버튼을 화면 녹화. 실패 시 대체: `data/demo_fallback.html`.
- [ ] **T5.14 리허설 타이머 실측 2회** *(PRE1)* — `07_발표대본_7분.md`의 누적시각과 대조. 30초 이상 밀리면 슬라이드 4 생략. 데모 구간만 따로 2회 계측.
- [ ] **T5.15 정답셋 2인 교차판정** *(AI5)* — `gold_set.csv`의 `judge1`·`judge2` 36행. 독립 판정자(에이전트) 대조는 31/31 일치로 `judge2_pass.csv`에 있으나 **사람 2인 판정은 아님** — 그대로 표기 중.
- [ ] **T5.16 최상위 README 갱신** *(구조 감사 H3)* — 기준일이 08-12이고 `06_앱/`·`docs/`가 폴더 구조에 없음. 「30초 요약」(무엇을 만들었나 / 실행 2줄 / 발표자료 위치) 신설, "코드는 F4 저장소" 안내 정정(실제 코드는 이 저장소 `06_앱/`).
- [ ] **T5.17 질의응답 6문항 답변 준비** *(기록 감사 §5)* — 심사위원 5인 중 3인이 AI 계열. Q11(`_auto_repair`가 기권 정확도를 자기충족적으로 만드는가) · Q12(홀드아웃 없는 오버피팅) · Q15(모델 이원화) · Q16(파인튜닝 미사용) · Q17(RAG 대신 프롬프트 계약) · Q18(비AI 기준선).
- [ ] **T5.23 P007 소관 부서 결측** *(시연 감사 중간)* — `P007 대학일자리플러스센터`의 `owner_dept`가 null이라 대본이 지목한 쌍에서 전화번호가 **한쪽만** 붙는다(P010도 동일). 원문에 "주관기관: 고용노동부 / 인천광역시 청년정책담당관"이 있으므로 추출 누락이다. 대본은 이미 "연락처가 함께 붙습니다"로 완화해 두었으나, 재추출로 채우는 편이 낫다.

## 5.4 범위 밖 — 운영 단계 (오늘 하지 않는다)

배포 감사가 산정한 **부서 1곳 실사용까지 개발 4~6주**. 발표에서는 로드맵으로만 말한다.

| 항목 | 규모 | 왜 오늘 안 하나 |
|---|---|---|
| 사내 서버 배포 (Dockerfile·프록시·상시 기동) | 2~3일 | 공무원 PC 직접 설치가 불가하므로 서버가 유일 경로 |
| 로그인·부서별 권한 | 3~5일 | 인증 코드 0줄. 사내망 노출 상태로는 보안심사 불가 |
| 정책 입력 UI (붙여넣기·HWP/PDF 업로드·저장) | 4~6일 (+파서 3일) | 사실상 제품의 절반. 시나리오 5가 여기 막혀 있음 |
| 다중 사용자 격리 (`cache_resource` 제거, `DETACH DELETE` 제거) | 2~3일 | 지금은 A의 조작이 B의 그래프를 파괴 |
| 판정 규칙 정밀화 (동일 부서 제외, 우선순위 스코어링, 상위 N컷) | 2~3일 | 정책 26건에 인계 공백 110쌍 — 100건이면 1,000쌍 단위로 소음화 |
| HWP 결재 서식 출력 | 3~4일 | 마크다운은 온나라에 올라가지 않음 |
| 수요신호 실데이터 교체 (고용24) | 조사 의존 | 4건 중 3건이 가상 표본 — "지원 공백" 판정의 근거 |
| LLM 경로 결정 (외부 API 승인 or 온프레미스) | 승인 절차 의존 | 망분리 심의 결과가 전제. **"불가능하다"고 단정하지 말 것 — 미확인** |

---

# Phase 6 — A3 3단계 정합성 (실사용자 워크스루로 발견)

**계기:** 제품책임자 지시로 "구현이 원래 문제정의·A의 공무원 시나리오에 맞는가"를 A3 전문 대조 + 실사용자 워크스루로 재검증했다.
**결과:** **치명 2건을 발견했고 둘 다 닫았다.** 상세: `03_제품설계/3단계_정합성_점검.md`

## 6.1 워크스루에서 드러난 것

청년정책담당관 주무관이 「청년 바이오 취업 브릿지」를 발의하려는 시나리오로 앱을 따라가 본 결과:

| # | 주무관 행동 | 결과 |
|---|---|---|
| 2 | **내 사업을 넣으려 한다** | ❌ 입력란이 URL 하나뿐 |
| 3 | URL을 찾는다 | ❌ **신규사업은 정의상 공고가 없다 — 여기서 막힘** |
| 6 | 검토서를 받는다 | ❌ 기존 사업끼리의 관계만, **내 사업이 한 줄도 없음** |

**즉 3단계 업무의 입력이 앱에 들어갈 수 없었다.** 지금까지 "시나리오 5 — 미구현"으로만 적어 두었으나 심각도를 잘못 매긴 것이었다. 이건 나중에 붙일 기능이 아니라 **3단계 업무의 입력 그 자체**다.

## 6.2 완료

- [x] **T6.1 신규사업(안) 텍스트 입력** 【치명 G1 해소】 사이드바 ①번 칸. 사업 개요를 붙여넣으면 기존 `extract_card()` → 규칙 → **내 사업 ↔ 기존 사업** 대조. 파이프라인은 이미 있었고 **입력 위젯 하나가 없었다.** 카드에 `검토 중인 사업안` 칩, 검토서 초안 맨 앞에 「1. 검토 대상 신규사업(안)」 절 생성.
- [x] **T6.2 수단(intervention_type) 축 도입** 【치명 G2 해소】 A3 반려사유는 "대상·**수단**·직무 동일"인데 수단을 비교하지 않고 있었다. 통제 어휘 7종(현금지원·현물/물품대여·교육훈련·상담/컨설팅·알선/매칭·시설/인프라·기업보조금) 추가.
- [x] **T6.3 null을 '차이'로 오인하던 버그** 한쪽만 값이 있는 것(=미확인)을 "대상 세분이 다르다"로 처리해 **진짜 중복을 '의도적 병행'으로 놓치고 있었다.** 양쪽에 값이 있고 다를 때만 차이로 인정하도록 수정.
- [x] **T6.4 '보완 관계' 판정 신설** 수단이 다르면 중복이 아니라 보완이다. 5번째 판정으로 분리해 화면·검토서·Cypher에 노출.
- [x] **T6.5 "제한없음" 명시와 언급 없음 구분** 원문이 "지원연령: 제한없음"이라 쓴 것은 정보이지 결측이 아니다. 0~200으로 정규화해 대상 차이 판정이 성립하도록 함.
- [x] **T6.6 채용지원 vs 사슬 밖 규칙 충돌 해소** 청년 신규채용을 조건으로 기업에 주는 지원은 시설 개보수가 포함돼도 채용지원이다.

**실증 (같은 신규사업안을 넣은 전후 비교)**

| 「청년 면접복장 지원」 신규안 | Phase 6 이전 | 이후 |
|---|---|---|
| vs 드림나래(면접정장대여) | ❌ "낭비 아님"으로 **놓침** | ✅ **조정 필요 중복** |
| vs 자격증 응시료 지원(현금) | ❌ 중복으로 **오탐** | ✅ 보완 관계 |

기존 코퍼스 판정도 바뀌었다 — `드림체크카드 ↔ 자격증 응시료`(둘 다 현금지원·같은 대상)가 **조정 필요 중복 후보로 처음 잡혔다.** 이전에는 이 지표가 항상 0건이어서 "3단계의 본론이 작동하지 않는다"는 감사 지적을 받았던 항목이다.

**측정 (n=37, 오답 0건):** 필드 100% · 기권 100% · 중첩 판정 100% · 인용 실재율 **81%**
인용 실재율은 91%→81%로 **내려갔다.** 필드가 늘어 인용 기회가 많아진 결과이며, 숨기지 않고 그대로 싣는다.

## 6.3 닫지 못한 격차 — 발표에서 먼저 말할 것

- **G3 협의 대상 라우팅** A3의 검토자는 사업 성격에 따라 고정된다(청년사업→청년정책담당관, 대학·RISE→교육협력담당관, 전부→평가담당관). 앱은 "두 정책의 소관 부서끼리"를 제시해 **같은 부서끼리 협조공문**을 내는 항목이 나온다. 필수 검토자인 평가담당관은 어디에도 없다.
- **G4 개편 근거(성과 미달)를 다루지 못함** A4 사례 1의 실제 경로는 "기존 일경험 사업의 실효성 한계 인식 → 개편"이었다. 우리 카드에 `outcome_kpi`(취업률·전환율)가 전 건 null이라(원문에 없음) **8단계 개편의 판단 근거를 만들 수 없다.**
- **G5 검토서 서식 미확인** 「유사·중복 사업 자체 검토서」에 정해진 서식이 있는지 확인하지 않았다. **"결재에 올리는 그 문서"라고 단정하지 말 것.**
- **P007·P010의 소관 부서·수단 결측** 원문에 있는데 추출이 놓친다. 화면에서 협의 대상이 한쪽만 표시된다.

## 6.4 발표 문구 교정 (Phase 6 결론)

| 지금까지의 문구 | 판정 | 교정 |
|---|---|---|
| "3단계 유사·중복 검토를 **대신한다**" | ❌ 과장이었음 | "3단계에서 주무관이 손으로 하던 **기존 사업 대조를 자동화한다**" |
| "결재에 올리는 그 문서" | ❌ 서식 미확인 | "검토서 **초안**" |

---

## 5.5 알려진 한계 — 발표에서 먼저 말할 것

숨기면 질의응답에서 터지고, 먼저 말하면 점수가 되는 항목들이다.

- **홀드아웃 셋이 없다.** 정답셋을 만든 쪽이 프롬프트도 고쳤으므로 42%→100%는 일반화 정확도가 아니라 동일 셋 반복 최적화다.
- **`_auto_repair`가 기권 정확도를 부분적으로 자기충족적으로 만든다.** null 필드를 `missing_fields`에 자동 기입하므로 "기권해야 함" 방향 케이스는 통과가 구조적으로 보장된다. 판별력은 양성 통제군(G32~G36) 쪽에만 있다.
- **`조정 필요 중복` 지표가 두 범위 모두 0건**이다. 현 코퍼스에 대상 요건이 완전히 겹치는 쌍이 없어서이며, 만들어 넣지 않았다.
- **풀 카드 16장 중 stage null 10장 / occupation null 8장** — 시설·R&D 사업이라 사슬에 참여하지 않는 게 맞지만, 화면에는 "사슬 밖"으로만 표시된다.
- **풀 카드의 인용은 원문이 아니라 B1 원장 재구성 텍스트 기준**이다. "원문 근거 인용"이라 뭉뚱그리면 과장이 된다.
- **인계 공백 110쌍 중 다수가 같은 부서끼리**인데 검토서 초안은 "부서 간 협조공문"을 붙인다.
