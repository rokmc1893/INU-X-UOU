"""A3 3단계 산출물 — 「유사·중복 사업 자체 검토서」 초안.

문서 이름은 A3 원문 그대로 쓴다. 이 도구는 **정책을 제안하지 않는다** — 발의권은 A1상
과(課) 단위이고 1단계 성립 결정은 담당 과장이다. 우리는 검토서 초안과 근거만 낸다.

**모든 판정은 '후보'로 적는다.** 조사자 C의 실패기준:
  "역할중첩 후보를 '확정'으로 표시하는 화면 요소가 하나라도 발견되면 즉시 되돌린다."
"""

SECTIONS = [
    "검토 대상 사업",
    "대조한 기존 사업",
    "판정 결과 (전부 후보)",
    "판정별 근거와 원문 인용",
    "미확인 항목과 그 이유",      # 비워 두지 않는다 — 못 본 것을 밝히는 칸이다
    "협의 요청 부서",
    "다음 창구와 마감일",
]


def draft(card, findings, coverage, track):
    """검토서 초안을 마크다운으로. 담당자가 결재문서에 붙여 쓴다."""
    raise NotImplementedError
